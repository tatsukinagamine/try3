<div class="container">
  <div class="col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
<%= form_for(@gen) do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <br>
  <%= f.submit %>
<% end %>
  </div>
</div>

<%= link_to "ツイッター一覧画面にもどる", gens_path %>
